{
  "name": "map-shine",
  "version": "1.0.0",
  "description": "A module to make the map shine.",
  "main": "dist/module.js",
  "scripts": {
    "build": "esbuild scripts/module.js --bundle --outfile=dist/module.js --format=iife --external:pixi.js"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@pixi/particle-emitter": "^5.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}