---
trigger: always_on
---

In module.js there is a line that begins with

 * @version x.x.xx

There is also the module.json in the root of the project which has a version number near the top and two version numbers for the manifest and download link at the bottom.

If I ask you to increment the version or prepare a new release I mean you need to update these values. Report back only once you edit all four locations where version numbers are stored.